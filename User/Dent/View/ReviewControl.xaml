<UserControl x:Class="Dent.View.ReviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dent.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" Width="800">
    <Grid Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="4.1*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Rectangle 
Fill="#FFA7A7A7" 
RadiusX="15" RadiusY="15"
Grid.RowSpan="2"/>
            <TextBlock Margin="10" FontSize="25" Foreground="White" Grid.Row="0" Text="{Binding ReviewsText}" TextWrapping="Wrap"/>
            <TextBlock Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Top" FontSize="18" Foreground="White" Margin="10" Text="{Binding ReviewsDate, StringFormat=HH:mm dd.MM.yyyy}"/>
        </Grid>
       
       
    </Grid>
</UserControl>
