<Window x:Class="Dent.View.NewsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dent.View"
        mc:Ignorable="d"
                Title="Главная страница" Height="450" Width="800" WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.7*"/>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <Grid Background="#97D4B9"/>
        <Grid Grid.Row="1" Background="#60BA99"/>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="1.5*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.6*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="2" Grid.RowSpan="2" Content="г. Уфа, ул. Менделеева, 4/2" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            
            <Image Grid.Column="0" Grid.RowSpan="2" Source="/Resources/зуб.svg" HorizontalAlignment="Left" Margin="10, 15"></Image>
            <Button Grid.RowSpan="2" Grid.Column="3" Content="Запись на приём" Padding="10" VerticalAlignment="Center"  Margin="10" Background="#FF60BA99" BorderBrush="{x:Null}" Foreground="WhiteSmoke" Click="Button_Click" >
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="15"/>
                    </Style>
                </Button.Resources>
                <Button.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleX="1"/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Button.RenderTransform>
            </Button>
        </Grid>


        <!--<Label Content="Family Dent" Foreground="WhiteSmoke" FontWeight="Medium"></Label>-->


        <Grid Grid.Row="1" Margin="80,10,80,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Content="Главная" x:Name="mainBtn" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#00DDDDDD" BorderBrush="#00707070" Click="mainBtn_Click"/>
            <Button Content="Услуги" HorizontalAlignment="Center" Click="MenuItem_Click" VerticalAlignment="Center" Grid.Column="1" Background="#00DDDDDD" BorderBrush="#00707070">
                
            </Button>
            <Button x:Name="reviewBtn" Click="reviewBtn_Click" Content="Отзывы" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="2" Background="#00DDDDDD" BorderBrush="#00707070"/>

        </Grid>

        <ListView x:Name="ListNews" Grid.Row="2"
          HorizontalContentAlignment="Stretch"
          ScrollViewer.HorizontalScrollBarVisibility="Disabled" Foreground="#FF3A3A3A">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="0 5"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
        <Frame Grid.Row="2" x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
    </Grid>

</Window>
